# Teaching Notes

- 用户偏好：中文、短句、明确的技术语言。
- 已有丰富 Java 编程经验和 Codex 使用经验；无需讲解变量、条件、函数等通用编程概念。
- 当前目标：2D 角色移动小 Demo。
- 教学策略：先用 GDScript 降低 Godot 入门摩擦；通过 Java 类比解释类型、继承和回调，但避免把节点误解成普通 Java 对象。

- 第一课实际项目在 `godot-learning/`。第二课沿用其 player.tscn；当前圆形碰撞半径约 87.3。
- 第二课已完成：静态墙壁与滑动碰撞。用户于 2026-09-13 确认完成；具体概念掌握情况尚未单独验证。
- 第二课复盘：用户将沿墙向上滑动解释为“有向上的力”。已解释本 Demo 设置的是速度，沿墙速度分量保留；尚未验证纠正后的理解。
- 第三课已完成：Area2D 触发区域与信号。用户于 2026-09-14 确认能穿过区域、只输出一次，并指出 Player 是 body，已理解本例使用 body_entered 的原因。离开后重新进入是否再次触发尚未单独反馈。
- 第四课已完成：`lessons/0004-enter-exit-feedback.html`。用户于 2026-09-14 确认“完成了，效果完全符合预期”，确认进入变色、停留保持、完全离开恢复、再次进入变色。依据为用户运行反馈；事件与属性值的概念解释已在第五课反馈中验证，Mask 实验仍未单独反馈。
- 第五课已完成：`lessons/0005-area-status-hud.html`。用户于 2026-09-14 确认“已完成，因为label的text不再有新事件进行修改”。已理解 text 保留上次赋值，停留时没有新事件修改它；同时补足第四课“事件与属性值”的概念验证。完成依据为用户反馈，不是代理交互验收；节点路径、画布层与碰撞层的概念未单独验证。
- 2026-09-14 起点核对：本机引擎为 `4.7.2.stable.official.ed1daf0bf`；保存项目 `/Users/sid/Documents/ChatGPT/Godot/godot-learning/world.gd` 已有第四课变色与恢复代码，1074 工作树的游戏文件仍为第三课状态。已从 01b6 工作树同步第四课课程、完成记录和相关导航修正，没有同步或覆盖游戏文件。
- 第五课材料验证：6 个课程/参考 HTML 的本地链接与锚点通过；3 道测验的正误分支及重复选择通过模拟 DOM 检查。提取完整示例，在 `/tmp` 临时场景用本机引擎验证初始、进入、停留、退出、重新进入、非 Player 过滤、CanvasLayer 默认值和 Label 最小尺寸，断言通过；启动有沙箱用户目录/日志权限及系统证书环境错误，无示例脚本报错。此检查不是用户游戏的交互验收。
- 浏览器视觉验收仍未完成：此前 file:// 被 URL 安全策略拒绝，本次未绕过；已请求用 Codex 文件面板展示第五课（返回 queued）。页面实际样式与浏览器按钮交互尚不能标记已验证。第五课已依据用户反馈确认完成；这不改变上述材料验证的范围。
- 第六课已准备，尚未完成：`lessons/0006-entry-count.html`。唯一成果为进入次数统计：进入加一、停留与离开保留、重新运行归零。复用第五课 HUD；聚焦 World 实例数据与显示结果、回调和实例生命周期的区别。只有用户确认后记录完成。
- 第六课起点：已只读核对保存项目包含第五课练习；课程分支已快进到远端 `5be72ea`，同步用户已有的变色与 HUD 游戏成果。未替用户实现第六课游戏练习。
- 第六课材料验证：7 个 HTML 页面本地链接与锚点通过；3 道测验正误分支与重复选择通过模拟 DOM 检查。提取课程完整脚本，在 `/tmp` 临时场景使用 Godot 4.7.2 验证 0 → 进入 1 → 停留 1 → 退出 1 → 再进入 2、非 Player 过滤、新 World 实例归零，断言通过。启动仍有沙箱用户目录/日志权限及系统证书环境错误，无示例脚本报错。浏览器控制重试后返回 `nodeRepl.fetch request failed`，因此浏览器视觉与真实按钮交互验收未完成；此验证不代替用户的游戏交互验收。
