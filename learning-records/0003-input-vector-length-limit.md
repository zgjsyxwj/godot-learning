# 已理解输入向量的长度限制

用户已正确理解 `Input.get_vector()` 会把输入向量的长度限制为不超过 1。键盘斜向输入若直接使用 `(1, 1)`，长度为 √2，速度会增加约 41%；长度限制避免了这项增幅，但不表示所有非零模拟输入都会变成单位向量。下一课可继续研究碰撞身体、碰撞形状和滑动移动，不必重讲这一概念。

**Evidence**：用户在第一课完成后已给出正确解释，由前一教学任务交接确认。
