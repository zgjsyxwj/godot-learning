# 第八课：按 R 重新开始已完成

用户于 2026-09-15 确认：“通过，r是整个instance重新加载了，但是普通回调还是在同一个instance”。

完成依据为用户反馈。用户已区分场景重载与普通回调的实例生命周期：重载用新的 World 及其子节点替换旧场景实例，字段重新初始化；普通回调仍作用于同一个实例，累计次数跨调用保留。这也补足第七课后尚待验证的生命周期理解。

用户未逐项复述验收链。本次未修改游戏代码，也未代替用户进行游戏交互验收。此前浏览器视觉验证限制仍保留，详见 NOTES.md。
